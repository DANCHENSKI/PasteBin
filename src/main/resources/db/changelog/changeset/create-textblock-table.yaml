databaseChangeLog:
  -  changeSet:
       id:  createTable-textBlock
       author:  DANCHENSKI
       changes:
         -  createTable:
              columns:
                -  column:
                     name:  id
                     type:  BIGINT
                     autoIncrement: true
                     constraints:
                       primaryKey: true
                       nullable: false
                - column:
                    name: text
                    type: varchar(200)
              schemaName:  pastebin
              tableName:  textblock